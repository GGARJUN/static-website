export const metadata = {
    title: "PCBA",
    description: "Get a better understanding of your manufacturing process.",
  };

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}